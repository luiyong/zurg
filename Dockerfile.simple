FROM ubuntu:20.04

# 安装运行时依赖
RUN apt-get update && apt-get install -y \
    libstdc++6 \
    && rm -rf /var/lib/apt/lists/*

# 创建应用目录
WORKDIR /app

# 复制编译好的应用
COPY build/standalone/Greeter ./Greeter

# 设置默认命令
CMD ["./Greeter", "--help"]